# Knowledge

Shared knowledge and memory platform services.

**Principles:**
- Handles document ingestion, chunking, embeddings, and vector storage.
- Provides retrieval and reranking capabilities to the `runtime`.
- Memory is treated as a shared platform service.
- Isolated from specific employee business logic; acts purely as a semantic search and storage layer.
